# 商家入驻全流程设计方案

> 项目：农家乐.cn（`xn--fjq23fx8l.cn`）  
> 技术底座：Python 静态生成 → `public/` → GitHub Pages  
> 目标：商家自助提交资料 → 后台审核 → **审核通过后全自动上线，无需人工二次操作**

---

## 0. 一句话结论

现在的站是**纯静态**的。静态站没有服务器，**存不了申请单，也存不了审核状态**。所以「审核通过后自动发布」这条链上缺两个东西：

1. **一个能读能写的审核台**（存申请单 + 记审核结果）
2. **一个能触发构建的钩子**（审核一通过就自动改数据、重建、部署）

本方案用 **飞书多维表格当审核后台 + GitHub Actions 当发布引擎** 补齐这两块。

**审核员唯一的人工动作 = 把状态改成「已通过」。** 之后全部自动，商家在 1 分钟内出现在网站上。

---

## 1. 现状断点（为什么现在做不到）

| 环节   | 现状                                               | 能否承载需求                 |
| ---- | ------------------------------------------------ | ---------------------- |
| 商家填表 | `templates/join.html`：7 字段（4 必填）                 | ❌ 字段远不够渲染一个详情页         |
| 提交   | 前端 `fetch` → 飞书群机器人 webhook                      | ⚠️ **只能"发消息"，不能存、不能读** |
| 存储   | 无                                                | ❌ 没有"申请单"这个概念          |
| 审核   | 无                                                | ❌ 只能在群里肉眼刷消息           |
| 发布   | 手工改 `data/shops.json` → `python build.py` → push | ❌ 全靠人工，正是要消掉的环节        |

### 关键认知：飞书自定义机器人是单向的

消息推进群里就散了 —— **拿不回来、改不了、查不到状态**。想"审核"，必须有结构化存储。这是整个方案的地基，绕不过去。

---

## 2. 整体架构

```
[商家端]                    [审核层]                      [发布层]
前端表单 + 图片上传
      ↓ POST                  ← ★ 起步可整段省掉：直接用飞书表单视图收表+收图
轻中转（云函数 / WorkBuddy）★可选
      ↓ 飞书 OpenAPI 写记录
                飞书多维表格「入驻申请」
                状态 = 待审核
                      ↓ Base 自动化推卡片
                ┌─ 老孟在 Base 里审 ─┐
                │                    │
          改成「已通过」        改成「已驳回」+ 填原因
                │                    │
                ↓ 调 repository_dispatch      ↓ 通知商家
         GitHub Actions:                       · 短信（自动，★起步不做）
          拉 Base「已通过」→ upsert 数据          · 电话（人工，高价值）
          → 拉图压 webp 传图床 → build.py → push     · 站点查询页
                ↓ 回写状态「已发布」+ URL
              网站自动多出这家商家 ✅
```

**三层职责：**

| 层   | 用什么            | 职责               | 成本 |
| --- | -------------- | ---------------- | --- |
| 接入层 | 飞书表单视图（起步）/ 前端表单 + 轻中转 | 收资料、传图片、防刷、落库    | **0 元** / 约 0.43 元/月 |
| 审核层 | 飞书多维表格 Base    | 存申请单、承载审核台、驱动自动化 | **0 元**（免费版 2 万行） |
| 发布层 | GitHub Actions | 拉数据、构建、部署、回写结果   | **0 元**（公开仓库免费） |



---

## 3. 入驻表单字段清单

### 3.1 设计原则

1. **表单字段 = 网站渲染字段，一一对应。** 多收一个字段就是多一道放弃的理由。
2. **必填只放"能让页面跑起来"的字段。** 其余选填、可跳过。
3. **不收敏感证件。** 不采集身份证、营业执照 —— 砍掉的是门槛，也是保管责任（见 3.2 D 组）。


### 3.2 完整字段表

**A 组 · 基本信息（必填，网站公开展示）**

| 字段    | 键名         | 必填 | 校验规则                        | 公开      |
| ----- | ---------- | -- | --------------------------- | ------- |
| 农家乐名称 | `name`     | ✅  | 2–20 字；需与门头招牌一致              | ✅       |
| 所在城市  | `city`     | ✅  | 下拉选择（当前 6 城）                | ✅       |
| 所在区县  | `district` | ✅  | 2–10 字，如「德清县」               | ✅       |
| 详细地址  | `address`  | ✅  | 10–60 字                     | ❌ 仅审核核验 |
| 经营类型  | `type`     | ✅  | 下拉：农家乐/乡村民宿/采摘园/垂钓园/亲子农场/其他 | ✅       |
| 人均消费  | `price`    | ⬜  | 数字，20–2000 元/人              | ✅       |

**B 组 · 联系方式（必填，不公开）**

| 字段    | 键名               | 必填 | 校验规则                 | 公开   |
| ----- | ---------------- | -- | -------------------- | ---- |
| 经营电话  | `phone`          | ✅  | **人工回拨确认**（起步路线）/ 短信验证码（自建表单路线） | ✅ 展示 |
| 联系人姓名 | `contact_name`   | ✅  | 2–10 字               | ❌    |
| 联系人手机 | `contact_phone`  | ⬜  | 留空则同经营电话             | ❌    |
| 微信号   | `contact_wechat` | ⬜  | 用于后续触达和补资料           | ❌    |



> **电话验证：本方案里性价比最高的一个环节 —— 但起步不必用短信**
>
> 短信验证码的价值是真的：① 验证电话真实可用（农家乐最常见的驳回原因就是"电话打不通"）② 反垃圾（脚本过不了验证码）③ 拿到一个**已验证的触达渠道**，后续驳回通知、补资料提醒全靠它。
>
> **但起步有更省的做法**：审核本来就是人工的，初审时**直接拨一次电话**即可 —— 这一通电话同时完成了上面三件事，还额外确认了"对方知道自己是这家农家乐"。而且**人工电话比短信更准**：空号、停机、转语音信箱，短信验证码都验不出来，电话一拨就知道。
>
> **什么时候值得上短信验证码**：申请量上来、脚本刷单开始出现时。成本极低 —— 0.05 元/条，1000 条起售 50 元能验 1000 个商家，那时它换来的反垃圾 + 自动触达就划算了。
>
> ⚠️ **一个硬约束**：飞书原生表单视图**不支持自定义验证码**。所以走"飞书表单起步"这条路时，电话核验只能靠人工回拨 —— 这反过来也是"起步不做短信"的又一个理由。

**C 组 · 展示素材（决定能不能通过）**

| 字段      | 键名             | 必填 | 校验规则                                         | 公开        |
| ------- | -------------- | -- | -------------------------------------------- | --------- |
| 门面/环境照片 | `photos[]`     | ✅  | **≥3 张**，≤5MB/张，jpg/png/webp；需 1 张门头 + 2 张环境 | ✅         |
| 简要介绍    | `intro`        | ✅  | 60–200 字                                     | ✅         |
| 特色标签    | `tags[]`       | ✅  | 多选，**≤3 个**（亲子/采摘/垂钓/湖景/烧烤/山景…）              | ✅ 驱动列表页筛选 |
| 设施服务    | `facilities[]` | ⬜  | 多选（停车/空调/独立卫浴/无线网/可做饭/带娃设施…）                 | ✅         |
| 房型与价格   | `rooms[]`      | ⬜  | 可重复组，≤6 组；每组：房型名/床型/面积/价格/备注                 | ✅         |

**D 组 · 资质 —— 已取消，不采集**

原方案在这里放营业执照、身份证、食品经营许可证。**现已整体砍掉。**

**为什么不收：**

| 角度 | 说明 |
|---|---|
| 门槛 | 农家乐大量是个体经营，很多没有执照。要证件 = 直接砍掉一大半潜在商家 |
| 责任 | 收了身份证和执照，平台就承担了**保管责任**。一旦泄露，是法律责任，不是技术事故 |
| 无用 | 证件真伪核验成本很高（要对接工商接口）。收了大概率也是"看一眼存着"，起不到筛选作用 |
| 合规 | 身份证属敏感个人信息，收集需单独同意 + 最严格的存储要求，与"填个表就能上"的产品定位直接冲突 |

**那审核凭什么判断真假？** 用**公开信息交叉验证**替代索要证件 —— 对商家零负担，对平台反而更可靠：

| 核验手段 | 怎么用 | 强度 |
|---|---|---|
| **电话确认** | 打经营电话，问"是不是 XX 农家乐"，看能不能对上 | 强（**起步阶段这是唯一的电话核验手段，也是最强的一条**） |
| **地图核验** | 高德/百度搜名称或地址，看有没有这个 POI | 强 |
| **公开平台交叉验证** | 大众点评 / 美团 / 抖音 / 小红书搜名称，看是否有对应门店且信息吻合 | **最强**，等于用第三方已核验过的信息 |
| **门头照片比对** | 照片里的招牌 ≠ 填的名称 → 存疑 | 中 |

> **这比收证件更好。** 证件只能证明"这家有执照"，交叉验证能证明"这家真的在营业、真的活在网上的评价里"。后者才是用户关心的。

**代价：E 组的真实性承诺必须加重。** 不收证件之后，承诺条款是唯一的约束，要写清"信息不实 → 永久下架 + 拉黑手机号"。

**E 组 · 合规承诺（必填）**

| 字段          | 键名            | 必填   | 说明 |
| ----------- | ------------- | ---- | ---- |
| 信息真实性承诺     | `agree_truth` | ✅ 勾选 | 不收证件后，这是唯一的约束条款。需写明不实的后果 |
| **图片使用授权**  | `agree_photo` | ✅ 勾选 | 同意平台存储、压缩、展示其上传的照片（照片要外置存储，必须有这条） |
| 服务条款 / 隐私政策 | `agree_terms` | ✅ 勾选 | |

### 3.3 表单分步设计（4 步）

必填集中在 1–3 步，**第 4 步整体可跳过** —— 农家乐老板普遍不擅长电脑，一次要 10 张照片，转化率会掉得很厉害。砍掉资质组之后，第 4 步只剩房型一项，跳过它不影响上线。

| 步骤    | 内容                  | 必填数 | 期望耗时 |
| ----- | ------------------- | --- | ---- |
| 第 1 步 | 基本信息（A 组）           | 5   | 2 分钟 |
| 第 2 步 | 联系方式（B 组，起步不做验证码）    | 2   | 1 分钟 |
| 第 3 步 | 照片 + 介绍 + 标签（C 组上半） | 3   | 3 分钟 |
| 第 4 步 | 房型与价格（可选，可跳过）       | 0   | 可跳过  |

**必须配「保存草稿」**（存后端，键 = 手机号）。原因见 3.4。

### 3.4 一个容易被忽略的细节：草稿

农家乐老板填表的环境往往是**手机 + 现场**：拍着照片、被客人叫走、回来页面已经刷新没了。没有草稿保存，就是直接流失。

- 第 2 步验证手机号后即创建草稿记录
- 后续每步自动存（防抖 3 秒）
- 草稿状态 `draft`，**不进审核队列**
- 给一个链接可随时续填：`/join/?t=<token>`（审核员在 Base 里点一下就能生成）

### 3.5 反垃圾（四层）

| 层 | 手段                                       | 起步是否可用 |
| - | ---------------------------------------- | ------- |
| 1 | 蜜罐字段（现有 `<input name="company_url">`，保留） | ✅ 自建表单  |
| 2 | 同手机号 24h 内限 1 次提交                    | ✅ 自建表单  |
| 3 | 同 IP 每小时限 5 次                            | ✅ 自建表单  |
| 4 | 短信验证码（B 组，必填）                            | ⬜ 自建路线再加 |

> **起步阶段的不对称优势**：反垃圾其实没那么要紧 —— 因为**审核是人工的，垃圾提交顶多脏了 Base 表，进不了网站**。飞书表单自带基础防护（可设填写权限 / 需登录），配合人工审核足够用。等脚本刷单**真的出现**了，再补云函数 + 验证码那层也不迟 —— 别为一个还没发生的问题提前付出开发成本。

---

## 4. 后台审核设计

### 4.1 审核台载体：飞书多维表格（Base）

选它的理由：

- 老孟已在用飞书，**手机上就能审**（审核大多发生在碎片时间）
- 表格界面即审核工作台，**零开发**
- 自带「自动化」能力，能调 webhook → 直接触发发布
- 有 OpenAPI，构建脚本能读
- 免费额度够用

### 4.2 Base 表结构

**「入驻申请」主表**（商家提交的原始数据全在这）

| 字段名                             | 类型     | 用途                                                     |
| ------------------------------- | ------ | ------------------------------------------------------ |
| 申请编号                            | 自动编号   | 对外唯一凭证，商家凭它查进度                                         |
| 状态                              | 单选     | `待审核` / `待补充` / `已通过` / `已发布` / `已驳回` / `已下线` / `发布失败` |
| 提交时间                            | 创建时间   | 排序依据                                                   |
| ——以上系统字段，以下业务字段——               |        |                                                        |
| 名称 / 城市 / 区县 / 详细地址 / 经营类型 / 人均 | 文本·数字  | 对应 A 组                                                 |
| 经营电话 / 联系人 / 联系人手机 / 微信         | 文本     | 对应 B 组                                                 |
| 照片                              | **附件** | 对应 `photos[]`。**审核看图的唯一入口**，通过后才转入图床               |
| 介绍 / 特色标签 / 设施服务 / 房型           | 文本·多选  | 对应 C 组                                                 |
| **图片 URL 列表**                    | 文本     | 发布时回写图床地址（多行文本），供 `shops.json` 引用                    |
| 承诺勾选                            | 复选框    | 对应 E 组（含图片使用授权）                                        |
| **slug**                        | 文本     | 审核通过时生成，如 `zhuli-renjia`，决定 URL                        |
| **驳回原因**                        | 多选     | 结构化，见 §7                                               |
| 补充说明                            | 文本     | 审核员手写                                                  |
| 上线地址                            | 文本     | 发布后回写                                                  |
| 发布时间                            | 日期     | 发布后回写                                                  |

**辅助表（建议）**

| 表      | 作用                                  |
| ------ | ----------------------------------- |
| 审核日志   | 每次状态变更留痕（谁、何时、从什么状态到什么状态）—— 出了问题能倒查 |
| 驳回原因字典 | 驳回原因的分类主数据，供主表多选引用                  |

### 4.3 三个必配视图

| 视图      | 类型              | 筛选                      | 用途                                  |
| ------- | --------------- | ----------------------- | ----------------------------------- |
| **待我审** | **Gallery（卡片）** | 状态 ∈ {待审核, 待补充}，按提交时间升序 | **主战场**。审核 90% 是看照片，卡片视图一眼能看完，表格做不到 |
| 已发布     | 表格              | 状态 = 已发布                | 查已上线商家                              |
| 驳回 / 失败 | 表格              | 状态 ∈ {已驳回, 发布失败}        | 盯异常                                 |

> 「待我审」**必须用 Gallery 视图**，字段顺序配成：照片 → 名称 → 电话 → 地址 → 介绍。审核时眼睛只看一列，不要左右拉表格。

### 4.4 审核动作清单（人工核验 7 条）

**第 4 条是替代「要证件」的核心手段** —— 不收营业执照之后，用第三方平台已核验过的信息来交叉验证，比看证件更直接。

| # | 核验项        | 判据                        | 不通过→      |
| - | ---------- | ------------------------- | --------- |
| 1 | 电话能确认      | 当场拨一次，对方能说清是这家            | 信息存疑      |
| 2 | 名称与门头一致    | 照片里的招牌 ≈ 填的名称             | 信息存疑      |
| 3 | 地址能落地      | 高德/百度搜得到这个 POI            | 信息存疑      |
| **4** | **公开平台有迹** | **点评 / 美团 / 抖音 / 小红书搜得到，且名称、地址吻合** | **信息存疑（重点）** |
| 5 | 照片是实拍      | 无其他平台水印、无网图、无过度修图         | 素材不合格     |
| 6 | 不是重复收录     | 按「电话 + 地址」在已发布里查重         | 涉嫌重复      |
| 7 | 介绍无绕平台信息   | 无手机号/微信号/二维码/外链           | 内容违规      |

**目标：每单 30 秒内可决断。** 达不到就说明字段或视图设计有问题，回来改。

> 第 4 条只对**拿不准的**做，不需要每单都查。第 1、3 条全绿且照片真实的，基本可以直接批。

### 4.5 提效：AI 预审预检

Base 里加一个只读字段「预检」，用飞书自动化或外部脚本自动打标：

| 预检项       | 规则                   | 结果      |
| --------- | -------------------- | ------- |
| 电话格式      | 位数/号段合法              | ✅ / ⚠️  |
| 照片数量      | ≥3                   | ✅ / ⚠️  |
| 介绍字数      | 60–200               | ✅ / ⚠️  |
| 疑似联系方式    | 正则命中手机号/微信/二维码       | ⚠️ 高亮   |
| 地址与城市一致性  | 地址文本含所选城市/区县         | ✅ / ⚠️  |
| **疑似证件或隐私** | **命中「身份证/执照/证件」等词，或高长宽比卡片图** | **⚠️ 提醒商家撤回** |

最后一条是"不收证件"决定的配套动作 —— 商家不看说明、顺手把身份证当环境照传上来是很可能发生的。**收到即刻删除，不要让它在系统里停留。**

---

## 5. 状态流转

### 5.1 状态定义（7 态）

| 状态               | 含义              | 商家可见文案        |
| ---------------- | --------------- | ------------- |
| `draft` 草稿       | 填了一部分，未提交       | 待提交           |
| `pending` 待审核    | 已提交，等待处理        | 已提交，1 个工作日内核验 |
| `supplement` 待补充 | 资料不全，已通知补交      | 需补充资料（附原因）    |
| `approved` 已通过   | 审核过了，**正在自动发布** | 审核通过，正在上线     |
| `published` 已发布  | 已上线，有 URL       | 已上线 ✅         |
| `rejected` 已驳回   | 终态：虚假/不符/超范围    | 未通过（附原因）      |
| `offline` 已下线    | 上线后违规或商家申请下架    | 已下线           |
| `failed` 发布失败    | 自动发布出错，需人工介入    | 审核通过，上线延迟     |

> **`approved` 是个中间态**，通常只存在几十秒到 1 分钟（等 Actions 跑完）。但它必须独立存在 —— 否则"审核通过了但没上线"这种情况无处安放，出了故障查不出来。

### 5.2 流转表

| 从                        | 到            | 触发         | 执行者 | 自动动作                                  |
| ------------------------ | ------------ | ---------- | --- | ------------------------------------- |
| —                        | `draft`      | 手机号验证通过    | 系统  | 建草稿记录                                 |
| `draft`                  | `pending`    | 提交表单       | 商家  | 校验→入库→推群卡片提醒                          |
| `pending`                | `supplement` | 判定资料缺失     | 审核员 | 生成补充链接，**短信通知商家**                     |
| `pending` / `supplement` | `pending`    | 商家补交       | 商家  | 回队列，标记「已补交」                           |
| `pending` / `supplement` | `approved`   | **点击通过**   | 审核员 | **生成 slug → 调 `repository_dispatch`** |
| `approved`               | `published`  | 构建部署成功     | 系统  | 回写「上线地址 + 发布时间」                       |
| `approved`               | `failed`     | 构建/部署失败    | 系统  | 群内告警，**人工介入**                         |
| `failed`                 | `approved`   | 修复后重试      | 审核员 | 重新触发发布                                |
| `pending` / `supplement` | `rejected`   | 点击驳回 + 填原因 | 审核员 | **短信通知商家**（含原因）                       |
| `published`              | `offline`    | 违规 / 商家申请  | 审核员 | 触发构建移除页面                              |

### 5.3 流转图

见对话中的状态流转图。

### 5.4 三条硬规则

1. **`rejected` 是终态，但冷却 30 天可重新申请。** 否则同一家会被反复提交。
2. **状态只能沿表里的箭头走**，禁止跳变（比如不能从 `pending` 直接跳 `published`）。在 Base 里用「状态」字段的「受限于上一步」或自动化校验兜住。
3. **每次变更写一条审核日志。** 商家投诉"我明明通过了"时，这是唯一的证据。

---

## 6. 审核通过后自动发布（核心实现）

### 6.1 触发：飞书 Base 自动化

在 Base 配一条自动化：

```
触发条件：当「状态」字段变为「已通过」
执行动作：发送 HTTP 请求
  POST https://api.github.com/repos/<owner>/nongjiale/dispatches
  Headers:
    Authorization: Bearer <GitHub PAT>
    Accept: application/vnd.github+json
  Body:
    { "event_type": "publish-shop",
      "client_payload": { "recordId": "recXXXX", "slug": "zhuli-renjia" } }
```

### 6.2 执行：GitHub Actions

`.github/workflows/publish.yml`

```yaml
on:
  repository_dispatch:
    types: [publish-shop]     # 主触发：审核通过
  schedule:
    - cron: '0 */6 * * *'      # 兜底：每 6 小时扫一次漏网的
  workflow_dispatch:           # 兜底：手动重跑

jobs:
  publish:
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
      - run: pip install -r requirements.txt
      - run: python tools/sync_from_feishu.py   # 拉 Base「已通过」→ upsert 进 data/shops.json
      - run: python tools/fetch_photos.py       # 从 Base 拉图 → 压缩 webp → 传图床 → 回写 URL
      - run: python build.py                    # 生成 public/
      - run: python check.py                    # 复用现有 9 项自检闸门
      - run: git commit -am "publish: <slug>" && git push
```

选 GitHub Actions 的理由：**不依赖老孟的电脑开机**，免费额度足够。站点本来就在 GitHub Pages 上，是条最短路径。


### 6.3 五个必须处理的实现细节

**① 幂等 —— 以 `slug` 为主键 upsert**

`sync_from_feishu.py` 必须能重复跑而不产生重复商家。每次是 `upsert by slug`：有则更新、无则追加。定时兜底任务会和主触发撞车，**幂等是前提**。

**② slug 在审核通过时生成并写回 Base**

`竹里人家` → `zhuli-renjia`；冲突加后缀（`-2`）。**必须在审核通过那一刻定下来并存进 Base**，不能等到构建时现算 —— 否则商家改了名字，URL 就变了，已收录的搜索排名全丢。

**③ 照片走图床 —— 可以，但必须是自建对象存储，而且要分两个区** ⚠️

照片外置是对的（理由见下方），但有硬约束，先说要避开的坑。

**公共免费图床（imgur / sm.ms / 各类"免费图床"）绝对不能用：**

| 问题 | 后果 |
|---|---|
| 无 SLA，随时挂 | 图床一挂，**所有**商家详情页图片集体变裂图 |
| 防盗链 / 流量限制 | 站点一有流量就 403，用户看到空白 |
| 内容不可控 | 图床方可能二次压缩、加水印、插广告，甚至直接审核删除 |
| **国内可达性** | 部分公共图床在国内需要代理。本站面向国内用户，**图加载不出来等于页面废了** |
| 第三方持有 | 商家的门头照、环境照等于交给了不受你控制的第三方 |

**该用什么：自己的对象存储 + CDN。**

**先给结论：起步阶段这一步可以完全不花钱。** 三家官网资费（2026 年现行价）：

| | 腾讯云 COS | 七牛云 | 阿里云 OSS |
|---|---|---|---|
| **长期免费额度** | ❌ 无（仅新用户试用额度） | ✅ **10GB 存储 + 10GB CDN 回源流量 + 10 万 PUT + 100 万 GET，每月自动发放、永久有效** | ❌ 无 |
| 标准存储 | 0.099–0.13 元/GB/月 | 0.099 元/GB/月 | 约 0.12 元/GB/月 |
| 外网下行流量 | 0.50 元/GB | **0.26 元/GB** | 约 0.50 元/GB |
| CDN 回源流量 | 0.15 元/GB | 0.15 元/GB | — |
| 数据请求 | 0.01 元/万次 | 0.01 元/万次 | 0.01 元/万次 |

**算一笔账**：500 家商家 × 5 张 × 200KB（webp 压完）≈ **500MB**。七牛的 10GB 免费额度够放 **1 万家商家**。起步阶段这笔钱是 **0**。

**但七牛有个必须提前知道的坑** —— 它的免费流量是「**CDN 回源流量**」，不是「外网流出流量」，两者不通用：

| 访问方式 | 计费项 | 免费额度 |
|---|---|---|
| ✅ 图片走**绑定的 CDN 域名** | CDN 回源流量 | 10GB/月，免费 |
| ❌ 直接用源站域名直连 | 外网流出流量 | **无免费额度**，0.26 元/GB |

所以七牛的正确用法是**绑一个自己的二级域名走 CDN**（如 `img.xn--fjq23fx8l.cn`）—— 这套逻辑和你在 Cloudflare 那边完全一致。

**什么时候换成 COS**：商家过千、或有真实访问量之后。那时 COS 生态更顺（你做 400 电话，本来就有腾讯云账号），存储 + 流量月费也就几块到几十块。

**唯一会突然花钱的地方是流量费** —— 万一某个商家在抖音/小红书上爆了，图片被大量外站访问，账单会跳。两道保险必须开：

| 保险 | 作用 |
|---|---|
| **Referer 防盗链** | 只允许 `xn--fjq23fx8l.cn` 引用图片。**这一条能把"被别的站盗图刷流量"直接掐死，是最省钱的配置** |
| **余额预警** | 图不在仓库里，但 URL 指向外部域名 —— 图床欠费即全站裂图 |

> **为什么不用 Cloudflare R2**：R2 无出口流量费、单价便宜，你本来也在用 CF 管 DNS —— 看着很合适。但**国内访问绕不过去**，面向国内用户的站点不能拿它当图片源。同理排除 imgur / sm.ms 等公共图床。

**关键设计：分两个区，绝不直传正式路径。**

商家上传的图**不能直接进正式目录** —— 否则违规图、垃圾图在审核之前就已经在 CDN 上了，"审核"就没意义了。

```
temp/<recordId>/xxx.jpg      ← 商家上传，未审核。7 天未审自动清理
shops/<slug>/cover.webp      ← 审核通过后转正，永久保留
```

**转正 = COS 服务端 Copy**（同地域内复制，不走公网、不计流量），不是重新上传。

**完整图片管道：**

| 阶段 | 动作 | 说明 |
|---|---|---|
| 1 前端上传 | 校验格式/数量 → **前端轻压缩**（长边 2000px、质量 0.85）→ **剥离 EXIF** → 传 `temp/<recordId>/` | 轻压缩是为了不让 Base 附件被塞爆 |
| 2 写 Base | 图片附件同步存进 Base「照片」字段 | **审核要在 Gallery 视图直接看图**，这是审核效率的关键 |
| 3 审核通过 | Actions 从 Base 拉图 → 二次压缩（长边 1600px、webp q80）→ 传 `shops/<slug>/` → 删 `temp/` | 两次压缩：前端保 Base 容量，后端保站点性能 |
| 4 回写 | 图床 URL 写进 Base「图片 URL 列表」+ `shops.json` | |

> **EXIF 必须剥离。** 手机拍的照片默认带 GPS 坐标 —— 不剥离，商家店面的精确位置就跟着图片散出去了，还附带拍摄设备和时间。这是隐私问题，不是优化项。

> **文件名用随机 hash，不要用原名。** 否则能被遍历猜测（`01.jpg`、`02.jpg`…），未审核或已下线的图会被翻出来。

**为什么最终要放图床而不是仓库** —— 这是你问的"可以吗"的正面回答：

| 理由 | 说明 |
|---|---|
| **GitHub Pages 有硬限制** | 官方建议站点 ≤1GB、每月流量 ≤100GB。1000 家 × 5 张 × 400KB ≈ 2GB，**直接超** |
| 仓库不膨胀 | 图片不进 git，clone 和构建都快 |
| 换图不动构建 | 只换图时改 URL 即可，不必重走一遍构建 |
| 可加防盗链 | COS 配 referer 白名单，只允许 `xn--fjq23fx8l.cn` 引用，防别人盗图 |

**兼容点**：现有 `shops.json` 有 `art` 字段（1–8 的 SVG 插画索引）。**有真实照片就用照片，没照片继续回落到 SVG** —— 老数据不用改，新老混排也不违和。

**④ 字段白名单导出 —— 默认拒绝**

`sync_from_feishu.py` 里用**显式白名单**决定哪些字段能进站点，而不是"排除黑名单"：

```python
PUBLIC_FIELDS = {"slug", "name", "city", "district", "price", "tags", "phone",
                 "intro", "facilities", "rooms", "photos"}

def to_public(rec):
    return {k: rec[k] for k in PUBLIC_FIELDS if k in rec}
    # 详细地址 / 联系人 / 手机 / 微信 —— 结构上进不来
```

理由：**白名单是默认拒绝。** 黑名单会漏 —— 以后 Base 里加一个新字段，忘了排除就自动泄了。不收证件之后这条依然要保留：**详细地址、联系人手机号、微信号同样不该出现在站点上**。

另外：Base 的「照片」附件字段**只用于审核看图**；正式站点引用的是图床 URL（见 ③）。

**⑤ 回写状态，闭环**

构建部署成功后，Actions 调飞书 OpenAPI 把该记录改成 `published`，写入「上线地址」和「发布时间」。这样审核台能直接看到结果；失败就改成 `failed` 并在群里 `@老孟` 告警 —— **绝不静默失败**。

### 6.4 失败与回滚

| 场景    | 处理                                           |
| ----- | -------------------------------------------- |
| 构建失败  | 状态 → `failed`，群内告警（附 Actions 日志链接）；不阻塞其他商家发布 |
| 数据格式错 | `sync_from_feishu.py` 校验失败即中止该条，其余继续（逐条隔离）   |
| 要下线一家 | 状态改 `offline` → 同时触发一次发布 → 该商家从数据里移除         |
| 误上线   | `git revert` 对应提交 + 重新部署；数据源以 Base 为准        |

**发布流程本身不产生不可逆操作** —— `git` 历史就是完整版本记录，任何一次发布都能回退。

### 6.5 降级方案（不碰 GitHub Actions 时）

如果 Actions 的 YAML + Secrets 配置觉得麻烦，可以先这样起步：

**用 WorkBuddy 定时自动化当发布守护进程。** 我建一个每 15 分钟的自动化任务：扫飞书 Base → 发现 `approved` → 本地跑 `build.py` → 推送。

|        | GitHub Actions    | WorkBuddy 定时 |
| ------ | ----------------- | ------------ |
| 依赖电脑开机 | ❌ 不依赖             | ✅ 依赖         |
| 配置难度   | 中（YAML + Secrets） | 低            |
| 适合阶段   | 目标形态              | **起步验证**     |

建议路径：**先用定时方案把链路跑通、验证数据管道，稳定后再迁到 Actions。** 见 §8。

---

## 7. 审核不通过的反馈处理

### 7.1 驳回必须给结构化原因

不给原因，商家就会反复提交同样的问题，审核成本翻倍。

**驳回原因分类**（Base 里做成分组多选，可多选）：

| 组         | 原因项                                 |
| --------- | ----------------------------------- |
| **资料不全**  | 照片不足 3 张 · 缺联系电话 · 缺详细地址 · 缺经营类型    |
| **信息存疑**  | 电话打不通 · 名称与门头不符 · 地址无法核实 · 与已收录商家重复 |
| **素材不合格** | 照片非实拍 · 带其他平台水印 · 清晰度不足 · 明显网图      |
| **范围不符**  | 非农家乐业态 · 所在城市暂未开通 · 已停业             |
| **内容违规**  | 简介含联系方式 · 含违规广告词 · 涉黄涉赌             |

每条原因配一句**标准话术**，直接用于短信通知，不用审核员现编。

### 7.2 通知渠道（按可靠性排序）

| 优先级 | 渠道        | 自动化   | 说明                                                     |
| --- | --------- | ----- | ------------------------------------------------------ |
| 1   | **短信**    | ✅ 全自动 | 阿里云/腾讯云短信。模板：「【农家乐.cn】您的入驻申请需补充资料：照片非实拍。请点击 xxx 继续提交。」 |
| 2   | **电话**    | ❌ 人工  | 高价值商家直接打过去，转化率远高于短信。建议只对"值得争取但卡在素材"的单子用                |
| 3   | **站点查询页** | ✅ 全自动 | 商家凭「申请编号 + 手机号」在 `/join/status/` 查进度与驳回原因              |

> ⚠️ **必须说清的约束**：飞书自定义机器人**只能往老孟自己的群里发消息，不能给外部商家发**。  
> 所以"自动通知商家"只能靠**短信 API**或人工电话，飞书在这里帮不上忙。这是最容易想当然的地方。

### 7.3 站点查询页 `/join/status/`

给商家一个透明的进度入口，能砍掉大量"我提交了怎么还没消息"的电话：

```
输入：申请编号 + 手机号（双因子，防止爬别人信息）
输出：
  · 当前状态 + 时间轴（已提交 → 已通过 → 已上线）
  · 若「待补充」：缺什么、补交入口（带 token 的链接，7 天有效）
  · 若「已驳回」：驳回原因 + 30 天后可重新申请
  · 若「已发布」：上线地址直达
```

### 7.4 两条驳回路径的区别

|      | 走「待补充」     | 走「已驳回」        |
| ---- | ---------- | ------------- |
| 适用   | 资料不全、素材不合格 | 虚假信息、重复收录、超范围 |
| 原单   | **保留**，可续填 | 关闭            |
| 商家动作 | 补交后回队列     | 30 天后重新申请     |
| 理由   | 门槛问题，值得救   | 原则问题，不必救      |

**区分这两条，是审核效率的关键。** 把"照片不够"和"虚假信息"混成一个驳回，会导致前者被错杀。

---

## 8. 分阶段落地

| 阶段         | 做什么                                                     | 产出         | 预计    |
| ---------- | ------------------------------------------------------- | ---------- | ----- |
| **P0 今天**  | ① 建飞书 Base「入驻申请」表 + 3 个视图 ② 建**表单视图**收表收图（链接挂到站上「我要入驻」按钮）③ 配新记录推群提醒 | 能收单、能看图、能审 | 2 小时  |
| **P1 本周**  | ④ 打通「Base 改成已通过 → repository_dispatch → 拉数据 → build.py → push → 回写已发布」 | **核心需求达成** | 1–2 天 |
| **P2 上线后** | ⑤ 照片下载压缩管道（⚠️ Base 附件 URL 会过期，必须落地成自有文件）⑥ `/join/status/` 查询页 ⑦ 审核日志表 | 闭环 + 提效    | 1 周   |
| **P3 稳定后** | ⑧ 自建 4 步表单 + 云函数中转（替掉飞书原生表单）⑨ AI 预审 ⑩ 短信通知                | 品牌一致 + 无人值守 | 看情况   |

**关键：P0 + P1 就能达成核心需求**（提交 → 审核 → 自动发布），全程 **0 元**。P2/P3 是提效和保障，不阻塞上线。

> **这一版的路子和上一版不同**：上一版 P1 是"自建 4 步表单 + 云函数中转"，现在把这块**后移到 P3**。理由见附二 —— 飞书原生表单视图能直接收表收图，把最贵的一块开发省掉了，先拿它验证审核台好不好用。**先跑通闭环，再优化体验**。

---

## 9. 监控指标

放 Base 仪表盘，每周扫一眼：

| 指标        | 计算                                        | 健康值                  |
| --------- | ----------------------------------------- | -------------------- |
| 提交量       | 本周 `pending` 新增                           | —                    |
| **一次通过率** | 直接 `pending → approved` / 总提交             | **> 60%**，低了说明表单提示不清 |
| 平均审核时长    | `approved`.`时间` − `提交时间`                  | < 4 小时               |
| 驳回率       | `rejected` / 总提交                          | < 20%                |
| 补交后通过率    | `supplement → approved` / `supplement` 总数 | **> 70%**，低了说明门槛太高   |
| 发布失败率     | `failed` / `approved`                     | **= 0**，非 0 立即修      |
| 从审核通过到上线  | `published`.`时间` − `approved`.`时间`        | < 2 分钟               |

---

## 10. 与现有代码的衔接点

> **先分清路线**：下面这张表是**自建表单路线（P3）**的改动清单。**起步路线（飞书原生表单）要简单得多** —— 站点侧只动两处：`templates/join.html` 里「我要入驻」按钮改为跳转飞书表单链接；`build.py` 里新增 `tools/sync_from_feishu.py`。**前端 JS 一行都不用碰**，因为表单页面在飞书那边。

| 现有文件                  | 要改什么                                                                 |
| --------------------- | -------------------------------------------------------------------- |
| `templates/join.html` | 扩建为 4 步表单（19 字段）；加「保存草稿」；保留蜜罐字段；**不加任何证件上传入口**                   |
| `static/js/site.js`   | 提交逻辑从"发飞书卡片"改为"POST 到中转接口"；加分步校验、短信验证、草稿、**图片前端压缩 + EXIF 剥离**       |
| `data/site.json`      | `feishu_webhook` 之外新增 `api_base`（中转地址）、`cdn_base`（图床域名）              |
| `data/shops.json`     | 结构不变，新增 `photos[]` 字段（图床 URL）；有 `photos` 用照片，无则回落 `art`              |
| `build.py`            | 基本不用改；**新增 `tools/sync_from_feishu.py`**、**`tools/upload_to_cos.py`**  |
| `check.py`            | 新增两节：**构建产物不含非公开字段**；图床 URL 可达性抽查                                     |
| shop 模板               | `art`（SVG 插画）→ 优先用真实照片，无照片回落 SVG                                     |

**关于 `check.py` 的非公开字段断言** —— 成本很低，价值很高。不收证件之后，要防的是**详细地址和联系方式**泄漏：

```python
# 构建产物里绝不允许出现这些字段
FORBIDDEN = ["idcard", "license", "contact_phone", "contact_wechat", "营业执照", "身份证"]
for f in PUB.rglob("*.html"):
    t = f.read_text(encoding="utf-8")
    for w in FORBIDDEN:
        if w in t:
            bad(f"{f.relative_to(PUB)} 疑似泄漏非公开字段：{w}")
```



---

## 附：已定与待定

**已定（本版采纳）**

| 决定 | 影响 |
|---|---|
| **不收身份证、营业执照** | D 组整体取消。改用公开平台交叉验证 + 电话确认替代证件核验；真实性承诺条款加重 |
| **照片走自建对象存储 + CDN** | 排除公共免费图床。分 `temp/` 与 `shops/` 两个区，审核通过才转正；前端压缩 + 剥离 EXIF |

---

## 附二：这三项到底花不花钱

**一句话答案：三项理论上都收费，但起步阶段全都花不到钱 —— 其中两项可以整个砍掉。**

### 三项的费用实况

| 项 | 有无免费额度 | 起步实际支出 | 结论 |
|---|---|---|---|
| **对象存储** | 七牛：10GB 永久免费；腾讯云：仅新用户试用，无长期免费 | **0 元** | 用七牛免费额度起步 |
| **中转层** | 腾讯云云函数：前 3 个月免费，第 4 个月起约 0.43 元/月 | **0 元** | **可以直接砍掉**（见下） |
| **短信** | 无免费额度。0.041–0.05 元/条，最低 1000 条起售 ≈ 50 元 | **0 元** | **起步直接不做** |

### 中转层可以整个砍掉 —— 这是本轮最大的发现

原假设是"商家提交 → 得有个接口接住 → 写进 Base"，所以要架一层云函数。但其实不必：

**飞书多维表格自带「表单」视图** —— 生成一个公开填表链接，**原生支持附件字段（收照片）**，商家填完数据直接进 Base 表。**不需要任何中转代码、不需要云函数、不需要服务器。**

代价是表单长什么样由飞书决定，套不了我们自己设计的那套 4 步表单。取舍很清楚：

| 路线 | 样式 | 成本 | 何时用 |
|---|---|---|---|
| **飞书表单 + 跳转**（推荐起步） | 飞书原生，朴素但可用 | **0 元 / 0 开发** | P0–P1，先跑通闭环 |
| 自建 4 步表单 + 云函数中转 | 完全自定义，与站点一致 | 约 0.43 元/月 | 商家上量、或要品牌一致性时 |

**建议**：先用飞书表单把「提交 → 审核 → 自动发布」这条链跑通，验证审核台到底好不好用。跑顺了、觉得表单样式拖后腿，再自建 —— 那时前端代码是现成的，只补一层中转。

### 短信起步直接不做

三条理由：

1. **要报备**：签名 + 模板都要审核（工作日约 2 小时出结果，但流程要走一遍）
2. **要起售**：最低 1000 条 ≈ 50 元，起步根本用不掉
3. **你的本行就是电话**：初审直接打过去最自然，而且「电话能打通」本来就是最关键的核验项 —— 短信验证码在这里是重复动作

短信真正有价值是在**商家上量之后**（几百家、批量驳回通知、续期提醒）。那时候 50 元买 1000 条也完全不心疼。

### 成本随规模的变化

| 阶段 | 商家数 | 月成本 |
|---|---|---|
| 起步（P0–P1） | 0–50 家 | **0 元** |
| 增长 | 50–500 家 | **0 元**（七牛免费额度内） |
| 起量 | 500–2000 家 | **几元 – 几十元**（存储 + 流量） |
| 上规模 | 2000+ 家 / 有真实流量 | **几十 – 几百元**，这时才值得上 COS + CDN |

> **唯一的成本风险是流量费，不是存储费。** 500MB 存储一个月不到 5 分钱；真正会跳的是图片被大量外站访问。所以**防盗链 + 余额预警这两道保险，比省钱本身更重要**。

---

**待定（需要老孟拍板）**

1. **对象存储：起步用七牛（0 元）还是直接上腾讯云 COS？** —— 建议七牛起步，代码写成可切换（改一行 `cdn_base` 就能换）
2. **中转层：先用飞书原生表单（0 开发），还是直接自建 4 步表单？** —— 建议先用飞书表单把闭环跑通
3. **短信：确认起步不做？** —— 建议不做，靠电话 + `/join/status/` 查询页兜住

---

## 附三：三项待定已定，闭环已跑通（2026-09-15 深夜）

### 老孟的决定

| 待定项 | 决定 | 落地形态 |
|---|---|---|
| 中转层 | **用飞书原生表单** | 零代码。Base 自带表单视图出公开链接，商家免登录免注册直接填 |
| 短信 | **不做** | 改人工回拨：初审时拨一次，同时验证真实性 + 反垃圾 + 确认是本店 |
| 图床 | **七牛云** | 永久免费额度（10GB 存储 + 10GB CDN 回源流量），起步 0 元 |

### 已建成并验证的东西

**飞书端** —— Base「农家乐·商家入驻审核」，27 字段，3 视图（待审核表格 / **照片审核 Gallery** / 已发布表格）；表单 17 题、必填只 5 项；公开链接已用无头浏览器匿名验证过可打开（页面上"登录"字样 0 次）。

> 一个硬约束值得记下来：**飞书的 `checkbox` 字段不能作为表单题**（只允许 text / number / select / rating / location / datetime / user / attachment）。原设计里的三张勾选框因此合并成一个单选「确认与授权」。

**站点端** —— 自建表单整段撤掉，入驻页改成飞书入口卡片（前置清单 + 大按钮 + 扫码 + 电话兜底）。

**发布引擎** —— `tools/sync_from_feishu.py`。审核员唯一动作是把状态改成「已通过」，之后：白名单取字段 → 生成 slug → **下载照片并剥离 EXIF** → 传图床（未配密钥则落本地）→ 幂等 upsert 进 `data/shops.json` → 回写状态「已发布」+ 上线地址。

**触发方式** —— 改为 WorkBuddy 定时自动化**每小时**扫一次 Base，不碰 GitHub Secrets。（30 分钟表达不了，定时器最小粒度是小时。）

### 两个设计上的修正（与本文档前面的规划不同）

1. **`temp/` 隔离区不用做了。** 前面规划的两段式（`temp/` 未审核 → `shops/` 永久）预设了"前端直传对象存储"。但走飞书原生表单这条路，照片先落在 Base 的附件字段里，**只有审核人能看见——飞书本身就是隔离区**。照片在"审核通过"之前根本不会进对象存储，少一段、少一处要定期清理的垃圾。
2. **图片管道从 4 阶段缩到 3 阶段。** 阶段一（前端轻压缩）随自建表单一起推迟到 P3；起步就一条：飞书附件 → 剥离 EXIF + 压成 webp → 图床。

### 两个实现上的坑（都已处理）

1. **删原图失败会连累压缩结果。** 照片处理完要删掉原图，这一步在 Windows 上失败是常态（文件被占用 / 杀软扫描 / 回收站不可用）。早期写法把 `unlink` 和压缩放在同一个 `try` 里，一失败就整体"回落到用原图发布"——**结果是把 3–5MB 的原始手机照连同 GPS 坐标一起发了出去，还不报错**。现在清理与处理彻底分开：清理失败只警告。`tools/selftest_photos.py` 专门守这条。
2. **剥离 EXIF 之前必须先应用旋转。** 手机竖拍的照片像素其实是横的，靠 EXIF 的 Orientation 标记让浏览器转 90°。只剥不转，全站竖拍照集体躺倒。`ImageOps.exif_transpose` 必须在 `paste` 到新图之前调用。

### 数据里存对象 key，不存完整 URL

`shops.json` 里照片存 `shops/<slug>/01-a3f9.webp`，域名放 `data/site.json` 的 `photo_cdn`。原因是七牛新账号送的**测试域名只有 30 天有效期**，正式上线必须绑自己的域名——存 key 的话换域名只改一行配置，存完整 URL 就得改全部商家数据。站点构建对「本地路径 / 对象 key / 完整 URL」三种形态都能渲染，随时可切换。

### 照片上站的三道保险

- **必须走绑定的 CDN 域名。** 七牛免费给的是「CDN 回源流量」，直连源站域名按 0.26 元/GB 且无免费额度。
- **Referer 防盗链。** 只允许自家域名引用，掐死被盗图刷流量——这是唯一的成本风险点。
- **余额预警。** 图在外面，图床欠费即全站裂图。

### 还没做的

| 事项 | 说明 |
|---|---|
| 发布通道 | 项目**还不是 git 仓库**，`public/` 只在本地。要真上线得先建仓库 + 配 GitHub Pages |
| 触发器的推送步骤 | 自动化目前只做「同步 → 重建 → 自检 → 汇报」，不推送。发布通道好了再接上 |
| `/join/status/` 查询页 | P2，商家看不到自己的审核进度 |
| slug 风格统一 | 示例数据是短拼音（`zhuli-renjia`），脚本生成的是全拼连字符（`ce-shi-xi-pan-xiao-yuan`），得统一，否则 URL 风格乱。城市 slug 已统一为短拼音（`luohe`），商家 slug 待定 |

---

## 附四：入驻城市从 6 个扩到 337 个（2026-09-15 深夜）

### 为什么必须扩

原来「所在城市」只有 6 个选项（杭州、成都、苏州、北京、西安、广州）+「其他城市」。全国别的地方的商家，第一步就填不下去——不是审核卡他，是表单里根本没有他的城市。

### 口径：337 个

| 类别 | 数量 |
|---|---|
| 地级市 | 293 |
| 自治州 | 30 |
| 地区 | 7 |
| 盟 | 3 |
| **地级行政区小计** | **333** |
| 直辖市（区划上属省级，但商家填报时就是当城市填） | 4 |
| **合计** | **337** |

数据源：国家统计局统计用区划代码（经 `province-city-china` 整理）。港澳台不收录——农家乐是大陆业态。「省直辖县级行政区划」那 4 条是县级市的归集壳子，不是独立行政区，已剔除。

**尚未覆盖**：省直辖县级市（济源、仙桃、潜江、天门、文昌、石河子等）。它们在地级清单之外，目前靠「其他城市」兜底，审核时需人工补录。要不要单独收录，待定。

### 核心设计：入驻可选项 ≠ 城市落地页

这是这次唯一需要想清楚的地方。337 个城市**不能全部建页面**：

- 入驻可选项要**全量**——来者不拒，谁都能填
- 城市落地页要**有商家才建**——否则 337 个里 331 个是空页，等于给搜索引擎递一堆薄内容，反而拉低整站质量评估

所以拆成三个职责清晰的文件：

| 文件 | 管什么 | 谁维护 |
|---|---|---|
| `data/regions.json` | **存在性**：337 个城市的标准名、省份、URL slug | `tools/build_regions.py` 生成，不手改 |
| `data/cities.json` | **内容**：手写简介 + 标签，只放想认真做的城市 | 人工策展 |
| 城市页 | 只给**有商家**的城市建 | `build.py` 从 shops.json 自动派生 |

同一个城市在「飞书字段选项 / 飞书记录 / 网站页面」三层里可以处于不同状态，互不干扰。

效果：新城市第一个商家审核通过 → 城市页自动出现（简介走模板、标签为空）→ 攒够商家后把简介和标签补进 `cities.json` → 页面自动升级。全程不碰代码。

### 踩到的坑

**1. 拼音会读错字，而 slug 是永久 URL。** pypinyin 通用读音在 6 个地名上出错，全部影响可分享、可收录的地址：

| 城市 | pypinyin 给的 | 正确 | 说明 |
|---|---|---|---|
| 漯河 | `tahe` | `luohe` | 差得最远，「漯」被读成古水名的 tà |
| 朝阳 | `zhaoyang` | `chaoyang` | 地名读 cháo |
| 昌都 | `changdou` | `changdu` | 「都」读 dū |
| 克孜勒苏 | `kezileisu` | `kezilesu` | |
| 锡林郭勒 | `xilinguolei` | `xilinguole` | 「勒」读 lè |
| 阿勒泰 | `aleitai` | `aletai` | 同上 |

排查方法是跑 pypinyin 的 heteronym 模式，把 100 个含多音字的城市全列出来人工判一遍。修正表 + 31 条回归断言都固化在 `tools/build_regions.py` 里——**换拼音库版本或改剥名规则时会立刻报警**。

**2. 拼音撞车的 5 组。** 苏州/宿州、台州/泰州、宜春/伊春、玉林/榆林、福州/抚州，拼音完全相同。按 adcode 升序处理：首个用裸 slug，其余加省后缀。苏州保住了 `suzhou`，已有 URL 不受影响。陕西用官方拼法 `shaanxi`（与山西 Shanxi 区分）。

**3. 剥自治州简称不能用正则。** 「延边朝鲜族自治州」用 `([\u4e00-\u9fff]{2,4})族$` 贪婪匹配会把「延边朝鲜」整个吃掉，剥出空字符串。主体名和民族名都是汉字，正则分不开——必须用民族名白名单。

**4. 飞书读接口对选项截断在 50 个。** `field-get` / `field-list` / `form-questions-list` 都只返回前 50 个选项，写 338 个读回来还是 50 个。做了个对照实验确认：新建字段写 60 个选项，读回 50 个（T01→T50）——**证明截断在读取端，写入是完整的**。最终用无头浏览器渲染表单页面，把 338 个选项逐个核对，**命中 338/338、零缺失**。

**5. 表单选项样式必须从「纵向排列」改成「下拉」。** 该字段原来的 `option_display_mode` 是 1（纵向），337 项铺开会占满好几屏。已改为 0（下拉，可搜索）。

### 幂等与可重生成

```bash
python tools/build_regions.py            # 用 data/_source/ 缓存生成，并导出飞书字段定义
python tools/build_regions.py --fetch    # 重新拉取最新行政区划数据（区划调整时用）
python tools/build_regions.py --check    # 只校验：条目数、slug 唯一性、回归断言、与策展城市对齐
```

`check.py` 新增第 [11] 节守着这条链路：基准表条数、slug 唯一性、每个商家城市能否对上、飞书字段定义是否同源。


