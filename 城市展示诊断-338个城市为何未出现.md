# 为什么「338 个城市」在列表里看不到 —— 诊断报告

排查时间：2026-09-16
排查对象：农家乐.cn（xn--fjq23fx8l.cn）线上站点 + 飞书多维表格入驻表

---

## 一、结论先说

**不是渲染故障，是「城市列表」的取数口径问题。**

站点上所有城市列表（首页下拉、热门城市页、筛选 chips、404 快捷入口、sitemap 城市页）
取的都是**「有商家的城市」**，不是**「可入驻的城市」**。

- 可入驻城市：**337 个地级行政区**（飞书字段里 338 项，多一个兜底「其他城市」）
- 有商家的城市：**6 个**（杭州、北京、苏州、广州、成都、西安）
- 31 家商家全部落在这 6 个城市里 → 剩下 **331 个城市没有商家 → 一个入口都不生成**

---

## 二、实测数据

### 网站侧（线上 xn--fjq23fx8l.cn 实测）

| 位置 | 实测结果 | 数据来源 |
|---|---|---|
| 首页「城市」下拉 | 7 项（全部城市 + 6 城） | `build_home()` → `CITIES` |
| `/cities/` 热门城市页 | 6 张城市卡片 | 同上 |
| `/list/` 城市筛选 chips | 7 个 | `city_chips()` → `CITIES` |
| `404.html` 城市快捷入口 | 6 个 | 同上 |
| `sitemap.xml` 里 `/city/` | 6 条 | `CITIES` |
| `public/city/` 目录 | 6 个 | `build_city()` |

### 飞书侧（入驻表 Base `MxZlbHXgXaJkHxsb0kTcFCkJnae` / 表 `tblpndu1XRI64TsG`）

| 检查项 | 实测结果 | 判定 |
|---|---|---|
| 字段「所在城市」选项数 | **338**（分页 200 + 138，`total: 338`） | ✅ 完整 |
| 表单前端渲染 | 选项行完整，含末尾「阿勒泰」「其他城市」 | ✅ 正常 |
| `+field-get` 单次读 | 只返回 **50** 个 | ⚠️ 接口截断，非真值 |
| `+form-questions-list` 单次读 | 只返回 **50** 个 | ⚠️ 同上 |

> **最容易被误导的一点**：拿 `field-get` 或 `form-questions-list` 去数选项，永远只能数到 50，
> 看上去像「只写进去 50 个」或「压根没写进去」。实际字段里 338 个一个不少。
> 要数全，必须用带分页的接口：
>
> ```bash
> lark-cli base +field-search-options --base-token <token> --table-id <table> \
>   --field-id "所在城市" --limit 200 --offset 0 --as user
> # 返回体里的 data.total 才是真实总数
> ```

---

## 三、根因（代码位置）

`build.py` 第 175–182 行：

```python
# 只给「有商家」的城市建页面。
# 337 个地级行政区里绝大多数是空的，全建页等于给搜索引擎递一堆空页，
# 反而拉低整站质量评估。有商家才有内容，有内容才值得有页面。
_city_hits = {}
for _s in SHOPS:
    _city_hits[_s["city"]] = _city_hits.get(_s["city"], 0) + 1
CITIES = sorted((city_meta(sl) for sl in _city_hits), ...)
```

`CITIES` 完全由 `data/shops.json` 反推，`REGIONS`（337 条基准表）只用来做两件事：
校验城市名合法性 + 提供 slug/省份。**基准表里没有商家的城市，不会进入 `CITIES`**，
因此不会进入任何列表、不会建页、不会进 sitemap。

这个取舍是当初有意做的（避免 331 个空页稀释整站质量），但它的副作用就是：
**「能入驻的城市」和「网站看得到的城市」在数量上差了 55 倍**，从界面上看像是坏了。

`data/site.json` 的 `join_form_url` 指向飞书表单，`templates/join.html` 写的是
「全国 {{CITY_TOTAL}} 个地级市…都能入驻」（337），而首页写的是「已覆盖 6 座城市」——
这两个数字并存、没有互相解释，是困惑的直接来源。

---

## 四、排查方向（自查清单）

### 若怀疑是飞书侧

```bash
# 1. 数真实选项总数（看 data.total，不看数组长度）
lark-cli base +field-search-options --base-token <token> --table-id <table> \
  --field-id "所在城市" --limit 200 --offset 0 --as user --jq '.data.total'

# 2. 分页把 338 项全捞出来，跟 data/regions.json 逐一对账
#    基准表 337 项 + 「其他城市」= 338，缺一项都说明同步断了

# 3. 前端实证：无头渲染表单页，核对首项/末项是否都在 DOM 里
msedge --headless=new --virtual-time-budget=20000 --dump-dom "<表单分享链接>" > form.html
grep -c "base-component-select-list-editor-text" form.html
```

### 若怀疑是网站侧

```bash
# 线上实测各入口的城市条数
curl -sS "https://xn--fjq23fx8l.cn/"        | grep -c 'class="city-card"'   # 期望 6
curl -sS "https://xn--fjq23fx8l.cn/cities/" | grep -c 'class="city-card"'   # 期望 6
grep -c "/city/" public/sitemap.xml                                          # 期望 6

# 判断是「设计如此」还是「构建漏了」：比对商家覆盖城市 vs 产物城市目录
ls public/city/                     # 应与下方命令输出一致
python -c "import json;print(sorted({s['city'] for s in json.load(open('data/shops.json',encoding='utf-8'))}))"
```

两条输出一致 → 设计如此；产物少了 → 构建阶段有问题（回头看 `build.py` 里 `_city_hits` 与
`city_meta()` 的 slug 是否对得上 `data/regions.json`）。

### 若怀疑同步断了（飞书填了城市，网站没出现）

```bash
python tools/sync_from_feishu.py --dry-run   # 看有没有 Skip 掉的记录
# 常见 Skip 原因：城市填「其他城市」、或城市名不在基准表里
```

---

## 五、要改的话，三条路

| 方案 | 做法 | 成本 | 风险 |
|---|---|---|---|
| **A. 只扩筛选器** | 首页下拉/筛选 chips 列全 337 城市，但只有 6 城可点，其余置灰或指向入驻引导 | 低（约 1–2 小时） | 用户点了没内容，体验略空；下拉变长 |
| **B. 全量建页** | 给 337 个城市都生成页面 | 低（改一行） | **高**：331 个零内容空页，典型薄内容，拖累整站质量评估，不建议 |
| **C. 城市目录页（推荐）** | 新增一个 `/regions/` 目录页，按省份分组列出全部 337 城市；有商家的 6 个是链接，其余标「待开放」并挂「推荐/入驻」入口。sitemap 只多收这 1 页 | 中（约半天，含模板+样式+自检断言） | 低。既让用户找得到自己城市，又不产生空页 |

配套建议（不论选哪条）：把 join 页的「337 个地级市可入驻」和首页的「已覆盖 6 座城市」
在文案上串起来（例如首页加一句「已开放 6 城，全国 337 城可申请入驻」），
避免两个数字各说各话。

---

## 六、另需留意的三个小口径问题

1. `data/regions.json` 的 337 条里不含**省直辖县级市**（济源、仙桃、潜江、天门、石河子等约 20 个），
   目前靠飞书字段末尾的「其他城市」兜底。若这类商家变多，网站侧按城市聚合会全部落到一个 slug 上。
2. 建站口径是 337 + 1 = 338；对外文案目前混用「337 个地级市」和「338」，
   建议统一为「全国 337 个地级行政区（另有其他城市兜底）」。
3. 网站 slug 与飞书选项名之间没有强校验回路：飞书侧改选项名（如「阿勒泰」→「阿勒泰地区」）
   不会被 `check.py` 拦住，只会让同步阶段把记录 Skip 掉。`check.py` 目前只断言了
   「飞书字段定义与基准表一致」，走的是本地导出文件比对，不是线上字段实读。
